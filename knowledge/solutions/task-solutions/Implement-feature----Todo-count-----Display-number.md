# Solution: Implement feature: **Todo count** - Display number of items left

> Category: task-solutions
> Tags: 
> Created: 2025-12-27T12:03:14.356147
> Source: Implement feature: **Todo count** - Display number of items left

## Problem

Implement feature: **Todo count** - Display number of items left

## Solution

Now I have a clear picture:

## Research Summary

### Current State

**`/workspace/todo-app`** - **Fully implemented** with Todo count feature:
- Hook (`useTodos.ts:63`): `activeTodoCount = todos.filter((todo) => !todo.completed).length`
- UI (`App.tsx:49-51`): Displays `{activeTodoCount} item{activeTodoCount !== 1 ? 's' : ''} left`

**`/workspace/todo-project`** - **Partially implemented** (needs UI integration):
- Hook already has `activeCount` and `completedCount` computed with `useMemo` (lines 50-58)
- App.tsx is just a skeleton - needs the full UI implementation

### Implementation Pattern for Todo Count

1. **In the Hook** (`useTodos.ts`):
   ```typescript
   const activeCount = useMemo(
     () => todos.filter((todo) => !todo.completed).length,
     [todos]
   );
   ```

2. **In the UI** (`App.tsx`):
   ```tsx
   <span className="text-sm text-gray-600 dark:text-gray-400">
     {activeCount} item{activeCount !== 1 ? 's' : ''} left
   </span>
   ```

### Key Implementation Details

---
*Auto-generated by CLOPUS Knowledge Base*
