# Solution: Resolve ESLint warnings

> Category: task-solutions
> Tags: 
> Created: 2025-12-29T16:41:47.270221
> Source: Resolve ESLint warnings

## Problem

Resolve ESLint warnings

## Solution

All 4 ESLint warnings have been resolved:

1. **KanbanBoard.tsx:53** - Replaced dynamic loop-based sorting with explicit static property access for each status (`'todo'`, `'in-progress'`, `'done'`)

2. **useKanbanStore.ts:84** - Replaced `tasks[taskIndex]` array index access with `tasks.find()` and `tasks.map()` pattern in `moveTask` function

3. **useKanbanStore.ts:108** - Same refactoring applied to `updateTask` function

4. **main.tsx:6** - Replaced non-null assertion (`!`) with proper null check that throws an explicit error if root element is missing

Verification:
- ESLint passes with no warnings
- All 37 tests pass
- TypeScript compiles without errors

---
*Auto-generated by CLOPUS Knowledge Base*
